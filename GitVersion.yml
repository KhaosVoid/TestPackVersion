assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-format: '{Major}.{Minor}.{env:GITHUB_RUN_NUMBER}'
mode: Mainline
next-version: 0.7.0
branches:
  main:
    regex: ^main$
    increment: Patch
    is-mainline: true
ignore:
  sha: []
merge-message-formats: {}
