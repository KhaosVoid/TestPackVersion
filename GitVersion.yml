assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-format: '{Major}.{Minor}.{${{github.run_number}}}'
mode: Mainline
next-version: 0.7.0
branches:
  main:
    regex: ^main$
    increment: Patch
    is-mainline: true
ignore:
  sha: []
merge-message-formats: {}
